import React, { useEffect, useMemo, useState } from 'react';
import { createRoot } from 'react-dom/client';
import type { ConfigStatus, ImageUsageSummary, KeyStatus, KeyUsageSummary } from '../shared/types';
import './style.css';

type Audit = { id: number; key_id?: string; action: string; message: string; created_at: string };
type Filter = 'attention' | 'all' | KeyStatus;
type Lang = 'vi' | 'en';
const dict = {
  en: { title:'9router Key Manager', subtitle:'Daily token quota control. Set limit → watcher disables key when exceeded → daily reset can re-enable it.', refresh:'Refresh', loginTitle:'Admin access', password:'Password', unlock:'Unlock', wrong:'Wrong password', lang:'Language', logout:'Logout', publicCheck:'Public key check', check:'Check', keyInput:'Paste your API key', backAdmin:'Admin login', notFound:'Key not found', needs:'Needs attention', tokens:'Today tokens', req:'Requests', active:'Active keys', cost:'Cost', auto:'Auto-disable', flow:'Recommended flow', f1:'1. Pick a key', f2:'2. Set daily limit', f3:'3. Action = disable', f4:'4. Watcher locks until next UTC+7 day', healthy:'Everything looks healthy.', status:'Status', name:'Name', usage:'Quota used', daily:'Daily limit', window:'Window', action:'Action', last:'Last use', quick1:'Quick: daily limit 2× current', quick2:'50M/day + disable', limit:'Daily/token limit', reset:'Reset policy', expires:'Expires at UTC+7', save:'Save policy', saving:'Saving…', resetNow:'Reset window now', usageTitle:'Usage', total:'Total', prompt:'Prompt', completion:'Completion', models:'Models', audit:'Audit', noAudit:'No audit events.', setup:'Setup needed', imageUsage:'Image usage', imagesToday:'Images today', imagesTotal:'Total images', recentImages:'Recent image jobs' },
  vi: { title:'9router Key Manager', subtitle:'Quản lý quota token theo ngày. Đặt giới hạn → watcher tắt key khi vượt → qua ngày UTC+7 có thể tự bật lại.', refresh:'Làm mới', loginTitle:'Truy cập quản trị', password:'Mật khẩu', unlock:'Mở khóa', wrong:'Sai mật khẩu', lang:'Ngôn ngữ', logout:'Đăng xuất', publicCheck:'Kiểm tra key công khai', check:'Kiểm tra', keyInput:'Nhập API key của bạn', backAdmin:'Đăng nhập admin', notFound:'Không tìm thấy key', needs:'Cần chú ý', tokens:'Token hôm nay', req:'Requests', active:'Key active', cost:'Chi phí', auto:'Tự tắt key', flow:'Flow khuyến nghị', f1:'1. Chọn key', f2:'2. Đặt daily limit', f3:'3. Action = disable', f4:'4. Watcher khóa tới ngày UTC+7 tiếp theo', healthy:'Mọi thứ ổn.', status:'Trạng thái', name:'Tên', usage:'Đã dùng quota', daily:'Giới hạn/ngày', window:'Chu kỳ', action:'Hành động', last:'Dùng gần nhất', quick1:'Nhanh: limit/ngày = 2× hiện tại', quick2:'50M/ngày + disable', limit:'Giới hạn token/ngày', reset:'Chính sách reset', expires:'Hết hạn lúc UTC+7', save:'Lưu policy', saving:'Đang lưu…', resetNow:'Reset window ngay', usageTitle:'Sử dụng', total:'Tổng', prompt:'Prompt', completion:'Completion', models:'Models', audit:'Audit', noAudit:'Chưa có audit.', setup:'Cần setup', imageUsage:'Sử dụng ảnh', imagesToday:'Ảnh hôm nay', imagesTotal:'Tổng ảnh', recentImages:'Lần tạo ảnh gần đây' }
} as const;
async function api<T>(url: string, init?: RequestInit): Promise<T> { const res = await fetch(url, { headers: { 'Content-Type': 'application/json' }, credentials:'include', ...init }); if (!res.ok) throw new Error(await res.text()); return res.json(); }
const fmt = (n?: number | null) => n == null ? '—' : n.toLocaleString(); const pct = (n?: number | null) => n == null ? '—' : `${n.toFixed(1)}%`;
function vnDateTime(utc?: string | null) { if (!utc) return '—'; return new Intl.DateTimeFormat('vi-VN', { timeZone:'Asia/Ho_Chi_Minh', month:'2-digit', day:'2-digit', hour:'2-digit', minute:'2-digit' }).format(new Date(utc)); }
function toVnInput(utc?: string | null) { if (!utc) return ''; return new Date(new Date(utc).getTime()+7*60*60*1000).toISOString().slice(0,16); }
function fromVnInput(v: FormDataEntryValue | null) { if (!v) return null; return new Date(new Date(`${v}:00.000Z`).getTime()-7*60*60*1000).toISOString(); }
function attention(k: KeyUsageSummary) { return ['danger','expired','warning','unlimited'].includes(k.status); }
function recommendation(k: KeyUsageSummary, hardDisable: boolean | undefined, lang: Lang) { if (lang==='vi') { if(k.status==='danger') return hardDisable&&k.actionOnLimit==='disable'?'Đã/ sẽ bị khóa tới chu kỳ ngày tiếp theo.':'Tăng limit hoặc đặt action = disable.'; if(k.status==='warning') return 'Theo dõi usage; cân nhắc tăng daily limit.'; if(k.status==='unlimited') return 'Nên đặt giới hạn token/ngày để tránh dùng quá mức.'; if(k.status==='expired') return 'Gia hạn hoặc thay key này.'; if(k.status==='inactive') return 'Key inactive; không nên có traffic.'; return 'Không cần hành động.'; } if(k.status==='danger') return hardDisable&&k.actionOnLimit==='disable'?'Locked until next daily reset if watcher runs.':'Raise limit or set action to disable.'; if(k.status==='warning') return 'Watch usage; consider raising the daily limit.'; if(k.status==='unlimited') return 'Set a daily token limit to prevent runaway usage.'; if(k.status==='expired') return 'Extend expiry or replace this key.'; if(k.status==='inactive') return 'Inactive; no traffic should use this key.'; return 'No action needed.'; }

function PublicCheck({ lang,setLang }: { lang: Lang; setLang:(l:Lang)=>void }) { const t=dict[lang]; const [key,setKey]=useState(''),[result,setResult]=useState<KeyUsageSummary|null>(null),[err,setErr]=useState(''),[loading,setLoading]=useState(false); async function submit(e:React.FormEvent){ e.preventDefault(); setErr(''); setResult(null); setLoading(true); try{ setResult(await api<KeyUsageSummary>('/api/public/key-check',{method:'POST',body:JSON.stringify({key})})); }catch{ setErr(t.notFound); } finally { setLoading(false); } } return <main className="login"><form className="loginBox publicBox" onSubmit={submit}><h1>{t.publicCheck}</h1><label>{t.lang}<select value={lang} onChange={e=>setLang(e.target.value as Lang)}><option value="vi">Tiếng Việt</option><option value="en">English</option></select></label><label>{t.keyInput}<input value={key} onChange={e=>setKey(e.target.value)} autoFocus /></label>{err&&<p className="loginErr">{err}</p>}<button disabled={loading}>{loading?'…':t.check}</button>{result&&<div className="publicResult"><p><b>{result.name}</b> <span className={`pill ${result.status}`}>{result.status}</span></p><p>{result.keyMasked}</p><div className="stats"><label>{t.tokens}<b>{fmt(result.total)}</b></label><label>{t.usage}<b>{pct(result.percentOfLimit)}</b></label><label>{t.daily}<b>{fmt(result.tokenLimit)}</b></label><label>{t.last}<b>{vnDateTime(result.lastUsageAt)}</b></label><label>{t.action}<b>{result.actionOnLimit}</b></label><label>{t.window}<b>{result.resetPolicy}</b></label></div></div>}</form></main>; }
function Login({ lang,setLang,onOk }: { lang: Lang; setLang:(l:Lang)=>void; onOk:()=>void }) { const t=dict[lang]; const [pass,setPass]=useState(''),[err,setErr]=useState(''); async function submit(e:React.FormEvent){ e.preventDefault(); try{ await api('/api/auth/login',{method:'POST',body:JSON.stringify({password:pass})}); onOk(); }catch{ setErr(t.wrong); } } return <main className="login"><form className="loginBox" onSubmit={submit}><h1>{t.loginTitle}</h1><label>{t.lang}<select value={lang} onChange={e=>setLang(e.target.value as Lang)}><option value="vi">Tiếng Việt</option><option value="en">English</option></select></label><label>{t.password}<input type="password" autoFocus value={pass} onChange={e=>setPass(e.target.value)} /></label>{err&&<p className="loginErr">{err}</p>}<button>{t.unlock}</button></form></main>; }
function App(){ const [lang,setLang]=useState<Lang>((localStorage.getItem('9rkm:lang') as Lang)||'vi'); const t=dict[lang]; const [authChecked,setAuthChecked]=useState(false),[authed,setAuthed]=useState(false); const [keys,setKeys]=useState<KeyUsageSummary[]>([]),[audit,setAudit]=useState<Audit[]>([]),[images,setImages]=useState<ImageUsageSummary|null>(null),[config,setConfig]=useState<ConfigStatus|null>(null); const [error,setError]=useState(''),[saving,setSaving]=useState(''),[selected,setSelected]=useState<KeyUsageSummary|null>(null),[filter,setFilter]=useState<Filter>('attention'); useEffect(()=>localStorage.setItem('9rkm:lang',lang),[lang]); if (location.pathname === '/check') return <PublicCheck lang={lang} setLang={setLang} />; useEffect(()=>{api<{authenticated:boolean}>('/api/auth/status').then(r=>setAuthed(r.authenticated)).finally(()=>setAuthChecked(true));},[]);
async function refresh(){ if(!authed) return; try{ setError(''); const [c,k,a,img]=await Promise.all([api<ConfigStatus>('/api/config/status'),api<KeyUsageSummary[]>('/api/keys/usage'),api<Audit[]>('/api/audit'),api<ImageUsageSummary>('/api/images/usage')]); setConfig(c); setKeys(k); setAudit(a); setImages(img); if(selected) setSelected(k.find(x=>x.keyId===selected.keyId)??null); }catch(e:any){ setError(e.message??String(e)); } } useEffect(()=>{ refresh(); const id=setInterval(refresh,15000); return()=>clearInterval(id); },[authed]); const totals=useMemo(()=>keys.reduce((a,k)=>({total:a.total+k.total,req:a.req+k.req,active:a.active+(k.isActive?1:0),attention:a.attention+(attention(k)?1:0),cost:a.cost+k.cost}),{total:0,req:0,active:0,attention:0,cost:0}),[keys]); const visible=keys.filter(k=>filter==='all'?true:filter==='attention'?attention(k):k.status===filter); const selectedAudit=selected?audit.filter(a=>a.key_id===selected.keyId):[]; if(!authChecked) return <main/>; if(!authed) return <Login lang={lang} setLang={setLang} onOk={()=>setAuthed(true)} />; async function savePolicy(k:KeyUsageSummary,form:HTMLFormElement){const fd=new FormData(form); setSaving(k.keyId); try{await api(`/api/keys/${k.keyId}/policy`,{method:'PATCH',body:JSON.stringify({tokenLimit:fd.get('tokenLimit')?Number(fd.get('tokenLimit')):null,actionOnLimit:fd.get('actionOnLimit'),resetPolicy:fd.get('resetPolicy'),expiresAt:fromVnInput(fd.get('expiresAt'))})}); await refresh();}finally{setSaving('');}} async function quickDaily(k:KeyUsageSummary,limit:number){await api(`/api/keys/${k.keyId}/policy`,{method:'PATCH',body:JSON.stringify({tokenLimit:limit,resetPolicy:'daily',actionOnLimit:'disable'})}); await refresh();} async function resetWindow(k:KeyUsageSummary){if(!confirm(`Reset usage window for ${k.name}?`))return; await api(`/api/keys/${k.keyId}/reset-window`,{method:'POST'}); await refresh();} async function logout(){await api('/api/auth/logout',{method:'POST'}); setAuthed(false);} return <main><header><div><h1>{t.title}</h1><p>{t.subtitle}</p></div><div className="headActions"><select value={lang} onChange={e=>setLang(e.target.value as Lang)}><option value="vi">VI</option><option value="en">EN</option></select><button onClick={refresh}>{t.refresh}</button><button type="button" onClick={logout}>{t.logout}</button></div></header>{error&&<pre className="error">{error}</pre>}{config&&!config.ok&&<section className="setup"><h2>{t.setup}</h2>{config.errors.map(e=><p key={e}>{e}</p>)}</section>}<section className="cards"><div className="card primary"><span>{t.needs}</span><strong>{fmt(totals.attention)}</strong></div><div className="card"><span>{t.tokens}</span><strong>{fmt(totals.total)}</strong></div><div className="card"><span>{t.req}</span><strong>{fmt(totals.req)}</strong></div><div className="card"><span>{t.active}</span><strong>{fmt(totals.active)} / {fmt(keys.length)}</strong></div><div className="card"><span>{t.cost}</span><strong>${totals.cost.toFixed(4)}</strong></div><div className="card"><span>{t.auto}</span><strong>{config?.hardDisable?'ON':'DRY RUN'}</strong></div><div className="card"><span>{t.imagesToday}</span><strong>{fmt(images?.todayImages)}</strong></div><div className="card"><span>{t.imagesTotal}</span><strong>{fmt(images?.totalImages)}</strong></div></section><section className="flow"><b>{t.flow}</b><span>{t.f1}</span><span>{t.f2}</span><span>{t.f3}</span><span>{t.f4}</span></section><section className="attention"><h2>{t.imageUsage}</h2><div className="stats"><label>{t.imagesToday}<b>{fmt(images?.todayImages)}</b></label><label>{t.imagesTotal}<b>{fmt(images?.totalImages)}</b></label><label>Success<b>{fmt(images?.success)}</b></label><label>Errors<b>{fmt(images?.errors)}</b></label><label>Bytes<b>{fmt(images?.bytes)}</b></label></div><h3>{t.recentImages}</h3><div className="audit">{images?.events?.length?images.events.slice(0,20).map(e=><div key={e.id}><code>{vnDateTime(e.created_at)}</code> <b>{e.kind}</b> <span className={`pill ${e.status==='success'?'ok':'danger'}`}>{e.status}</span> {e.model} {e.size} {e.output_file&&<code>{e.output_file}</code>} {e.drive_path&&<code>{e.drive_path}</code>} {e.error&&<em>{e.error}</em>}</div>):<p>{t.noAudit}</p>}</div></section><section className="attention"><h2>{t.needs}</h2>{keys.filter(attention).length===0?<p>{t.healthy}</p>:keys.filter(attention).map(k=><button className={`issue ${k.status}`} key={k.keyId} onClick={()=>setSelected(k)}><b>{k.name}</b><span>{k.statusReason}</span><em>{recommendation(k,config?.hardDisable,lang)}</em></button>)}</section><section className="toolbar">{(['attention','all','danger','warning','unlimited','expired','inactive','ok'] as Filter[]).map(f=><button key={f} className={filter===f?'active':''} onClick={()=>setFilter(f)}>{f}</button>)}</section><section className="tableWrap"><table><thead><tr><th>{t.status}</th><th>{t.name}</th><th>{t.usage}</th><th>{t.tokens}</th><th>{t.daily}</th><th>{t.window}</th><th>{t.action}</th><th>{t.last}</th></tr></thead><tbody>{visible.map(k=><tr key={k.keyId} onClick={()=>setSelected(k)}><td><span className={`pill ${k.status}`}>{k.status}</span></td><td><b>{k.name}</b><br/><code>{k.keyMasked}</code></td><td><div className="meter"><div style={{width:`${Math.min(k.percentOfLimit??0,100)}%`}} /></div>{pct(k.percentOfLimit)}</td><td>{fmt(k.total)}</td><td>{fmt(k.tokenLimit)}</td><td>{k.resetPolicy}</td><td>{k.actionOnLimit}</td><td>{vnDateTime(k.lastUsageAt)}</td></tr>)}</tbody></table></section>{selected&&<aside className="drawer"><button className="close" onClick={()=>setSelected(null)}>×</button><h2>{selected.name}</h2><p><code>{selected.keyMasked}</code> <span className={`pill ${selected.status}`}>{selected.status}</span></p><p className="reason"><b>{selected.statusReason}</b><br/>{recommendation(selected,config?.hardDisable,lang)}</p><div className="quick"><button type="button" onClick={()=>quickDaily(selected,Math.max(selected.total*2,1_000_000))}>{t.quick1}</button><button type="button" onClick={()=>quickDaily(selected,50_000_000)}>{t.quick2}</button></div><form onSubmit={e=>{e.preventDefault(); savePolicy(selected,e.currentTarget);}}><label>{t.limit}<input name="tokenLimit" type="number" min="1" defaultValue={selected.tokenLimit??''}/></label><label>{t.reset}<select name="resetPolicy" defaultValue={selected.resetPolicy}><option value="daily">daily</option><option value="monthly">monthly</option><option value="manual">manual</option><option value="custom">custom</option></select></label><label>{t.action}<select name="actionOnLimit" defaultValue={selected.actionOnLimit}><option value="disable">disable</option><option value="alert">alert</option><option value="none">none</option></select></label><label>{t.expires}<input name="expiresAt" type="datetime-local" defaultValue={toVnInput(selected.expiresAt)}/></label><div className="actions"><button disabled={saving===selected.keyId}>{saving===selected.keyId?t.saving:t.save}</button><button type="button" onClick={()=>resetWindow(selected)}>{t.resetNow}</button></div></form><h3>{t.usageTitle}</h3><div className="stats"><label>{t.total}<b>{fmt(selected.total)}</b></label><label>{t.usage}<b>{pct(selected.percentOfLimit)}</b></label><label>{t.prompt}<b>{fmt(selected.prompt)}</b></label><label>{t.completion}<b>{fmt(selected.completion)}</b></label><label>{t.cost}<b>${selected.cost.toFixed(6)}</b></label><label>{t.last}<b>{vnDateTime(selected.lastUsageAt)}</b></label></div><h3>{t.models}</h3><pre>{JSON.stringify(selected.models,null,2)}</pre><h3>{t.audit}</h3><div className="audit">{selectedAudit.length?selectedAudit.map(a=><div key={a.id}><code>{a.created_at}</code> <b>{a.action}</b> {a.message}</div>):<p>{t.noAudit}</p>}</div></aside>}</main>; }
createRoot(document.getElementById('root')!).render(<App />);
