# 9router Key Manager

Local-only dashboard/API for managing 9router API key usage windows, quota metadata, expiry, watcher alerts, and optional hard-disable actions.

> Work in progress. Do not commit real `~/.9router` data.
